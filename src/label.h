#ifndef LABEL_H
#define LABEL_H

#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include"types.h"

LABEL *InsertLabel(LABEL *Label, ArborState *State);
LABEL *CreateLabel(ArborState *State);

#endif

